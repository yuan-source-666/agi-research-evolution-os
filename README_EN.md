# AGI Research & Evolution OS (Experimental Personal Learning Project)

> **[English](README_EN.md) | [中文](README.md)**

> **Disclaimer: This repository is generated by an AI assistant under my guidance.**
> The code, documentation, and commits were mostly produced and organized by AI.
> The content only represents a personal learning exploration, not mature research — use with caution.
> All instance IDs, access tokens, and internal addresses have been replaced with `REDACTED_*` placeholders.
> See the full [DISCLAIMER.md](DISCLAIMER.md) for details.

This is a spare-time self-evolving LLM learning experiment, run on a Hygon DCU instance
on SCNet (National Supercomputing Internet, China). The idea: use a small local model
(Qwen2.5-1.5B → 7B) as the "brain", and try to compensate for its limitations with
program-side architecture — tool calling, reflection loops, persistent memory, and an
evolution proposer.

## What Was (and Wasn't) Achieved

**Achieved:**

- A chat pipeline: local relay (`relay/chat_relay.py`) → SCNet Jupyter kernel → model
- A DRAFT → CRITIQUE → REVISE → LEARN reflection loop with persistent memory files
- Phase B–E experiment scripts and result JSONs (evolution proposer, knob tuning, LoRA fine-tuning, etc.)
- v5.x: a program-side numeric guard inspired by DeepSeek-R1's published chain-of-thought
  style (premise anchoring, natural backtracking), which intercepts most numeric drift on test questions

**Not achieved / known issues:**

- The 7B base model has a weight-level numeric drift bias (minimal experiment: with no
  scaffolding at all, it recalls 10 as 11). Program-side guards can only intercept output;
  they cannot fix the root cause.
- Phase experiments have very small sample sizes; conclusions are preliminary observations
  without statistical significance.
- The code is fast-iteration experiment scripting: no tests, no linting, many hardcoded paths.

## Repository Layout

| Directory | Contents |
|---|---|
| `relay/` | Local relay core (`chat_relay.py` chat gateway, `rsh.py` remote execution, monitoring page) |
| `relay/ops/` | Ops scripts: daemon deployment / restart / status checks / archive sync |
| `relay/experiments/` | Local side of experiments: Phase launchers, diagnostic probes, bionic-LLM experiments |
| `remote/` | Cloud core: agi_core / agi_engine / evolution_loop / tool_system / memory_system / world_model, plus serving scripts and state JSONs |
| `remote/experiments/` | Phase B–E experiment scripts (executed in the cloud) and their result JSONs |
| `remote/v8/` | Bionic LLM v7/v8 and DCU ablation experiments |
| `remote/agi_phase1/` | Phase-1 evolution loop prototype |
| `docs/` | `RESTORE.md` (rebuild-from-scratch manual), environment info, pinned deepseek-harness commit |

> Note: the repository layout has been reorganized for readability. Paths inside
> `docs/RESTORE.md` refer to the original working archive, so they may differ from
> the directory names here (e.g. `relay/ops/` and `relay/experiments/` were flat under
> `local_scnet_v8/` in the original archive).

## Restore / Reproduce

See `docs/RESTORE.md`. Requirements: Python 3.11 / PyTorch 2.9 (ROCm/DCU),
Qwen2.5-series models, and the deepseek-harness commit pinned in
`docs/deepseek_harness_commit.txt`.

## License

For learning and communication only; code is provided under MIT (no warranty of any kind).
See [DISCLAIMER.md](DISCLAIMER.md).
